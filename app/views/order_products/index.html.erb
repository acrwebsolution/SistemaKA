<p id="notice"><%= notice %></p>

<h1>Listing Order Products</h1>

<table>
  <thead>
    <tr>
      <th>Codpedprod</th>
      <th>Codcliente</th>
      <th>Codproducto</th>
      <th>Cantidad</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_products.each do |order_product| %>
      <tr>
        <td><%= order_product.codpedprod %></td>
        <td><%= order_product.codcliente %></td>
        <td><%= order_product.codproducto %></td>
        <td><%= order_product.cantidad %></td>
        <td><%= link_to 'Show', order_product %></td>
        <td><%= link_to 'Edit', edit_order_product_path(order_product) %></td>
        <td><%= link_to 'Destroy', order_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order product', new_order_product_path %>
